# ========== 行业轮动预测系统配置文件（优化版 V2）==========
# 针对 IC 接近 0 和验证集不稳定的问题，系统性优化方案
# 目标：提升模型学习能力，提高 IC 和稳定性

# ========== 数据配置 ==========
# 使用优化后的特征集（20个核心特征，聚焦信号，减少噪声）
data_config: "classify/config_data_industry_baseline.yaml"

# ========== 模型配置（优化版）==========
# IndustryGRU 模型配置
industry_gru_config:
  num_features: null  # 自动从数据中推断
  sequence_length: 60  # 时序长度：60天
  
  # 🔴 优先级1：增强模型容量（提升学习能力）
  hidden_size: 128  # ⬆️ 从 64 增到 128（增加模型容量，提升学习能力）
  num_layers: 2  # 保持 2 层（多层可以学习更复杂的模式）
  attention_hidden_dim: 128  # ⬆️ 从 64 增到 128（增强特征注意力能力）
  
  # 🟠 优先级2：调整正则化（平衡容量和过拟合）
  dropout: 0.15  # ⬇️ 从 0.2 降到 0.15（减少正则化，让模型有更多学习空间）
  weight_decay: 1e-4  # 保持当前值
  
  # 🔵 优先级3：优化损失函数（提升排序学习能力）
  loss: "ranking"  # 保持 Ranking Loss
  ranking_alpha: 0.8  # ⬆️ 从 0.7 增到 0.8（更强调排序，减少对绝对值的依赖）
  
  # 🟡 优先级4：优化训练策略
  batch_size: 32  # ⬇️ 从 64 降到 32（更小的 batch 可能有助于学习）
  lr: 0.001  # ⬆️ 从 0.0007 增到 0.001（加快学习速度）
  weight_decay: 1e-4  # 保持
  grad_clip: 1.0  # 保持梯度裁剪
  lr_scheduler: "plateau"  # 保持学习率调度器
  max_epochs: 100  # ⬆️ 从 80 增到 100（给模型更多学习时间）
  patience: 15  # ⬆️ 从 12 增到 15（更耐心的早停，避免过早停止）

# ========== 滚动训练配置（优化版）==========
rolling:
  train_months: 24  # 保持 24 个月（足够的训练数据）
  valid_months: 2  # ⬆️ 从 1 增到 2（更稳定的验证集，减少波动）
  test_months: 1  # 测试窗口：1个月
  step_months: 1  # 滚动步长：1个月
  min_samples: 100  # 最小样本数

# ========== 训练集 IC 计算配置 ==========
train_ic:
  enabled: false  # 禁用训练集 IC 计算（节省时间）

# ========== 诊断配置 ==========
diagnosis:
  enabled: true  # ⬆️ 启用诊断（帮助定位问题）

# ========== IC 日志配置 ==========
ic_logging:
  window: 60  # IC 计算窗口
  half_life: 20  # 半衰期

# ========== 路径配置 ==========
paths:
  model_dir: "data/models/industry_rotation"
  log_dir: "data/logs/industry_rotation"
  prediction_dir: "data/predictions/industry_rotation"
  backtest_dir: "data/backtest/industry_rotation"

# ========== 组合配置 ==========
portfolio:
  max_position: 1.0  # 最大仓位
  max_industry_weight: 0.15  # 单个行业最大权重
  top_k: 10  # 选择排名前 K 个行业

# ========== 回测指标配置 ==========
backtest_metrics:
  - "IC"
  - "Rank IC"
  - "Long-Short Return"
  - "Annual Return"
  - "Sharpe Ratio"
  - "Max Drawdown"

