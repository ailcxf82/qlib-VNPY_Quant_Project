"""REMOVED (stage G.2) — see ``factor_lab.adapters.quant_proposal``.

Any import of this module raises ``RuntimeError`` on purpose. Update to::

    from factor_lab.adapters.quant_proposal import (
        ProjectQlibQuantHypothesisGen,
        compose_project_rag,
        _STATIC_CONSTITUTION,
        _PROJECT_FACTOR_RAG,
    )
"""

from __future__ import annotations

raise RuntimeError(
    "rdagent_integration.project_quant_proposal 已在阶段 G.2 下线；"
    "请 import factor_lab.adapters.quant_proposal"
)
